/*CODEX_TASK_START*/
{
  "repo": "woosus/GGDEMY",
  "branch": "main",
  "message": "ci(pages): safe YAML fix (quote 'on', no tabs)",
  "files": {
    ".github/workflows/pages.yml": {
      "text": "name: Deploy to GitHub Pages\n'on':\n  push:\n    branches:\n      - main\n  workflow_dispatch: {}\npermissions:\n  contents: read\n  pages: write\n  id-token: write\nconcurrency:\n  group: 'pages'\n  cancel-in-progress: true\njobs:\n  build:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/upload-pages-artifact@v3\n        with:\n          path: .\n  deploy:\n    environment:\n      name: github-pages\n      url: ${{ steps.deployment.outputs.page_url }}\n    runs-on: ubuntu-latest\n    needs: build\n    steps:\n      - id: deployment\n        uses: actions/deploy-pages@v4\n"
    }
  }
}
/*CODEX_TASK_END*/
